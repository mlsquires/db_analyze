# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) { |repo| "https://github.com/#{repo}.git" }

gem "mls_utility", git: "https://github.com/mlsquires/mls_utility", branch: "add_executable"

# Specify your gem's dependencies in db_analyze.gemspec
gemspec

